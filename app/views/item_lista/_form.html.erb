<%= form_for(@item_listum) do |f| %>
  <% if @item_listum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@item_listum.errors.count, "error") %> prohibited this item_listum from being saved:</h2>

      <ul>
      <% @item_listum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :item_id %><br>
    <%= f.select :item_id, options_for_select(@items.map{ |c| [c.nombre, c.id, {'data-id'=>c.id}] }) %>
  </div>
  <div class="field">
    <%= f.label :listum_id %><br>
    <%= f.select :listum_id, options_for_select(@lista.map{ |c| [c.nombre, c.id, {'data-id'=>c.id}] }) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
